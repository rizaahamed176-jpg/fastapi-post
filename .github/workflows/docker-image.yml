name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build -t fastapi-post .
     -name: docker login 
     -with: ${{DOCKER_USERNAME}}
            ${{}}
     -name: docker push
     -run: docker push rizaahamed176
